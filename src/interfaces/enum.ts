// tabs ids
export enum TabIds {
  Text = 0,
  Style = 1,
  Effects = 2,
}

// Gradient direction
export enum GradientDirection {
  TO_R = "to_right",
  TO_L = "to_left",
  TO_B = "to_bottom",
  TO_T = "to_top",
  TO_BR = "to_bottom_right",
  TO_BL = "to_bottom_left",
}

// animation type
export enum AnimationTypes {
  FADE_IN = "fade-in",
  SLIDE_UP = "slide-up",
  BOUNCE = "bounce",
  SCALE = "scale",
  TYPEWRITER = "typewriter",
}
